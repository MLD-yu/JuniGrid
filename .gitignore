# .NET build artifacts
bin/
obj/
.vs/
*.user
*.suo

# stray junk
$null
NVIDIA Corporation/
*.bak
jh/
jh2/
vectrus/
_revert-backup-*/
index.html

# installer payload & build output (generated from publish/sc)
installer/JuniGridInstaller/payload.lz
installer/JuniGridInstaller/payload.zip
installer/publish/

# 打包用的离线依赖二进制（约 50MB）— 不进源码库；build-installer.ps1 会从
# installer/deps 读取，缺失时运行时再从官方源下载。Release 资产里已内嵌。
installer/deps/

# build outputs & local artifacts
dist-tmp/
objversioncheck-tmp/
publish/
.workbuddy/

# runtime logs
JuniGrid/run.log

# test docs (local only)
TestDocs/

# test harness run outputs
test-harness/*.txt
